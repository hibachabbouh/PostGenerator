services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: postgenerator-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./embeddings:/app/embeddings
      - ./data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: postgenerator-frontend
    environment:
      API_BASE_URL: http://backend:8000
    ports:
      - "8501:8501"
    depends_on:
      - backend
    restart: unless-stopped
